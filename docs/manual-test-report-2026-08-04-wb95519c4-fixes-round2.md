# 95519c4 修复回归手测报告 · 第二轮增量

> 执行日期：2026-08-04  
> 测试清单：`docs/manual-test-2026-08-04-wb95519c4-fixes.md`  
> 执行范围：仅复测第一轮 9 个 FAIL，跳过第一轮已 PASS 的 F7/F8/F10/F11/F13  
> 目标项结论：**4 PASS / 5 FAIL / 0 阻塞**

## 1. 构建与证据边界

- 开测时仓库与插件安装包哈希均为 `456d6187d1bd477ecf974e7b6e9624e75df65b8c3ec0a39e5be255e1865a28fb`，Obsidian 新进程 PID `99213`。
- 测试期间构建文件被再次更新为 `14520e76810cde86c4ddae7ad7dee3f779ff89b24bc820510433ee3cfd243f87`，但旧 Obsidian 进程没有自动加载新文件。
- 发现后已退出旧进程、恢复干净测试基线、重新部署，并以新进程 PID `38826` 加载 `14520e76…`。最终仓库与安装包哈希一致。
- F1、F6、F12 已在加载 `456d6187…` 的进程中 PASS；按照“直接跳过 PASS”的要求，未在 `14520e76…` 上重复执行。
- F2、F3、F4、F5、F9、F14 的最终结论来自加载 `14520e76…` 的进程。
- 积分不足期间出现的“出错了”、无响应和超时证据按用户说明全部作废；补充积分后的有效重试结果才纳入结论。
- 最新构建复测阶段模型为 `auto`，CLI 超时时长按用户要求设为 10 分钟。
- 未执行全量 CLI smoke：曾误启动，收到“跳过 PASS”纠正后立即终止，不作为证据。

## 2. 九个目标项结果

| 编号 | 结果 | 有效证据 |
|---|---|---|
| F1 自动标题连续对话 | **PASS** | `456d6187…`：标题生成后第二、三、四轮均正常；上下文问题回答的是当前会话，不串到标题会话。按要求未在后续构建重跑。 |
| F2 vault 外附件授权 | **PASS** | `14520e76…`：允许一次后真正发送并返回 `EXTERNAL-ACP-V2-MARKER-ONCE`；总是允许后返回 `...-ALWAYS` 并写入 `allowedExternalPaths`；同路径再次发送不弹窗且正常返回标记。取消路径在前阶段已通过。 |
| F3 Write 完成态 | **FAIL** | `14520e76…`：界面显示完全访问，但仍弹 Write 批准卡；允许后文件写入成功、路径保留，完成态仍没有默认折叠的全绿 diff。 |
| F4 Edit diff 与撤销 | **FAIL** | `14520e76…`：完全访问仍弹 Edit 批准卡；批准卡内红删绿增预览正确，文件首行实际变为“秋天来了”；完成态没有“撤销此修改”按钮。 |
| F5 默认批准链 | **FAIL** | `14520e76…`：批准卡、允许、`regression-check.md` 落盘及内容 `ok` 均正常；完成态仍没有 diff 块。 |
| F6 fork 后原会话 | **PASS** | `456d6187…`：分叉会话和切回后的原会话均正确回答暗号 `ALPHA`。按要求未在后续构建重跑。 |
| F9 thought_level | **FAIL** | `14520e76…`：high 消息正常；`/effort low` 即时同步；修改子代理 JSON 触发 CLI 重启后仍保持 low。但日志没有 `session/set_config_option` 或 `thought_level/high` 出站记录，三项门禁未全部满足。 |
| F12 Agent 去重 | **PASS** | `456d6187…`：正文只有一遍主代理总结；展开工具详情可见 `Agent` 行和“子代理输出”折叠块，中继全文可查且未重复进正文。按要求未在后续构建重跑。 |
| F14 日志三件套 | **FAIL** | `14520e76…`：弹窗能自动刷新，但始终只有启动、`initialize`、`session/load` 与一条“无归属会话 ... config_op”；缺少 prompt、set_config_option、fork、工具完成等本轮记录。 |

## 3. 仍需处理的 Issue

### P0

1. **WB-R2-001：完全访问模式仍弹 Write/Edit 批准卡**
   - F3、F4 均复现，授权模式按钮明确显示“完全访问”。
   - 说明 set_mode/会话激活修复仍没有覆盖当前新会话或工具请求链。

### P1

2. **WB-R2-002：Write 完成态 diff 仍缺失**
   - 完全访问 Write 与默认批准 Write 两条链均复现。
   - 文件实际落盘，批准卡保有 rawInput 和路径，但完成态没有折叠 diff。

3. **WB-R2-003：Edit 完成态撤销仍缺失**
   - 批准卡预览正确、文件也实际修改，但完成态没有撤销按钮，无法执行回滚验收。

4. **WB-R2-004：出站与工具/fork 日志仍未写入日志缓冲区**
   - 自动刷新 UI 已生效，但底层缓冲区内容仍停在启动阶段。
   - F9 的 high 下发缺少旁证，F14 也无法还原请求时序。

## 4. 验收结论

- 已确认修复：F1 自动标题后续对话、F2 外部附件三个允许路径、F6 fork 后原会话、F12 Agent 去重。
- 仍未修复：F3/F5 Write 完成态 diff、F4 Edit 撤销、F9/F14 出站日志。
- 当前不建议将第二轮补丁标记为全部验收通过。

## 5. 测试环境恢复

- `456d6187…` 与 `14520e76…` 两个阶段的测试结束现场分别归档到：
  - `/tmp/workbuddian-wb95519c4-r2-4PUkaD/demo-vault.after-456d`
  - `/tmp/workbuddian-wb95519c4-r2-4PUkaD/demo-vault.after-14520`
- 已恢复测试前 demo-vault；恢复前后均为 39 个文件。
- 除用户明确要求保留的 `cliTimeoutMinutes=10` 外，解析后的 `data.json` 与测试前完全一致。
- 已保留最新 `14520e76…` 插件包，仓库与安装包 SHA-256 一致。
- 已删除 Downloads 中本轮创建的两个外部附件测试文件；Obsidian 已正常退出。
